[project]
name = "augur"
version = "0.1.0"
description = "Claude Code-powered Feishu bot"
requires-python = ">=3.12"
dependencies = [
    "lark-oapi>=1.5.3",
    "claude-code-sdk>=0.0.25",
]

[build-system]
requires = ["setuptools>=68.0"]
build-backend = "setuptools.build_meta"
